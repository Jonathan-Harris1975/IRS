{
  "name": "image-redirect-service",
  "version": "1.0.0",
  "private": true,
  "description": "Validated Cloudflare Pages redirect registry for branded image URLs.",
  "scripts": {
    "check": "node scripts/validate.mjs",
    "audit:targets": "node scripts/check-targets.mjs",
    "scan:secrets": "node scripts/secret-scan.mjs",
    "watch:pages": "node scripts/watch-pages-deployment.mjs",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run scan:secrets && npm run lint && npm run check && npm test",
    "verify:release": "npm run verify && npm run audit:targets",
    "lint": "node scripts/lint.mjs"
  },
  "engines": {
    "node": "22.23.2",
    "npm": "10.9.8"
  },
  "packageManager": "npm@10.9.8"
}
